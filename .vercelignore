# Ignore files that are not needed for Vercel deployment
# These files are for local development or MCP server only

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Output files (generated at runtime)
output/

# Local config (use environment variables instead)
.env.local
.env.*.local

# Lock files - Vercel will use requirements.txt instead
uv.lock
poetry.lock
Pipfile.lock

# Temporarily hide pyproject.toml to force Vercel to use requirements.txt
# This prevents Vercel from trying to use uv.lock which doesn't exist
# pyproject.toml is only needed for MCP server local development
pyproject.toml
